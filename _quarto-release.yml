# Local-only export profile. GitHub Pages runs the default HTML format from
# scripts/build.sh; these formats intentionally stay out of deployment.
book:
  downloads: [pdf, epub]

format:
  epub:
    toc: true
    toc-depth: 3
  typst:
    toc: true
    toc-depth: 3
