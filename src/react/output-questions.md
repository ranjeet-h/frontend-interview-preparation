# Output Questions

> **Unique content from `05`.** Canonical hook theory → [Hooks In-Depth](hooks.md).
> Implementation challenges → [Coding Challenges](coding-challenges.md).

"What will be the output?" questions covering all React concepts:

- `useState` and re-renders — batching, stale closures
- `useEffect` and side effects — dependency array traps
- `useMemo` & `useCallback` — when memoisation fires
- `useContext` — re-render propagation
- `useReducer` — dispatch and state updates
- Rendering and memoisation — `React.memo`, `useCallback` with children

---

{{#include ../../05-react-coding-challenges.md:735:1917}}
