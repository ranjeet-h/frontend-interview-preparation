# Advanced Patterns & Ecosystem

> **Canonical chapter** for advanced hooks and the React ecosystem.
> TypeScript with React, SSR/ISR strategies, and architectural design.

**Topics:**
- `useImperativeHandle` — customising exposed ref API
- Error Boundaries — catching render errors
- React Portals — rendering outside parent DOM
- `<StrictMode>` — what it does and why
- React + TypeScript — typing props, state, hooks
- **Rendering strategies** (canonical): SSR, CSR, SSG, ISR
- Architectural design: reusable, accessible Data Grid component

---

{{#include ../../04-react-theory-concepts.md:880:1165}}
