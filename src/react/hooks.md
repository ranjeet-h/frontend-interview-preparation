# Hooks In-Depth

> **Canonical chapter for React hooks.**
> Core concepts → [Core Concepts](core-concepts.md).
> Hook output Qs → [Output Questions](output-questions.md).

**Topics:**
- `useState` and `useEffect` — state and side effects in functional components
- Dependency array in `useEffect` — empty / populated / missing
- `useReducer` vs `useState`
- `useMemo` and `useCallback` — performance optimisation
- `useRef` vs `createRef` — beyond DOM access
- Custom hooks — extracting and reusing component logic
- `useLayoutEffect` vs `useEffect`

---

{{#include ../../04-react-theory-concepts.md:297:538}}
