# Guess the Output: Closures

A closure keeps the variable, not a snapshot of the value. These questions — especially the `var`-loop / `let`-loop / IIFE trio, which must be understood together — test whether you can see which environment each callback captured.
