# Guess the Output: The Event Loop

The mandatory section. Every question carries the full seven-step trace — synchronous execution, call stack, scheduled microtasks, scheduled macrotasks, drain order, next macrotask, final output — from single-timer basics up to combined challenges mixing timers, promises, microtasks, and `await`.
