# Guess the Output: Types, `typeof`, `NaN`, Objects as Keys

The small lies JavaScript tells: `typeof null` is `"object"`, `NaN` is a number that equals nothing (not even itself), and plain-object keys are all strings — so two different objects collide. `Map` is the contrast that proves the rule.
