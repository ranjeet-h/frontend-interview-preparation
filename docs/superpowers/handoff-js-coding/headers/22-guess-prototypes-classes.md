# Guess the Output: Prototypes & Classes

Property lookup walks the chain, assignment shadows it, and `class` is a constructor with the same prototype machinery underneath. Static fields live on the constructor, instance fields on the object — one question makes the two collide on purpose.
