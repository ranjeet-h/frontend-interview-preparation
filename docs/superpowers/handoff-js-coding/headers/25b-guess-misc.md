# Guess the Output: Descriptors, Arrays, References & Modules

Descriptor defaults that hide properties, `freeze`/`seal`/`preventExtensions` compared side by side, holes versus `undefined`, `for...in` indices versus `for...of` values, `const` bindings that still mutate, circular `JSON.stringify`, Date construction pitfalls, and module live bindings.
