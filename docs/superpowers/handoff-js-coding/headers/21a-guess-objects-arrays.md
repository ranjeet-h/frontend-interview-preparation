# Guess the Output: Objects & References

Destructuring defaults that apply to `undefined` but not `null`, spread that copies one level and shares the rest, `delete` that hollows arrays without shrinking them, and the three array methods people confuse: mutating `push`/`splice` versus copying `slice`.
