# Guess the Output: Scope & Hoisting

`var`, `let`, `const`, function declarations, and function expressions each initialise their binding at a different moment. Every question here is answered by the same two timelines: what exists after setup, and what each statement does during execution.
