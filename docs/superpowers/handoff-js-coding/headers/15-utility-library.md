# Utility / Library Questions

This page is the "lodash-shaped" framing of the utilities covered earlier: `get`, `set`, `once`, `memoize`, `cloneDeep`, `isEqual`, `groupBy`, `chunk`, `flatten`, `uniq`, `debounce`, and `throttle`. Where a utility is already implemented in full on another page, this page states the contract and cross-links instead of repeating the whole derivation.
