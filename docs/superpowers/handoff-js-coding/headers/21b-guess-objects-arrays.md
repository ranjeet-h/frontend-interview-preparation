# Guess the Output: Array Methods, Sorting, Set & Map

The callback-signature traps: a braced arrow that returns nothing, `filter` truthiness, and the famous `["1","2","3"].map(parseInt)`. Plus default lexicographic sort, `Set` deduplication, and `Map` keys that stay distinct where object keys collide.
